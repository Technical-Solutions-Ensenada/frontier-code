---
interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className = '', hover = true } = Astro.props;
---

<div class={`bg-white rounded-lg shadow-md border border-slate-200 ${hover ? 'hover:shadow-lg transition-shadow duration-200' : ''} ${className}`}>
  <slot />
</div>
